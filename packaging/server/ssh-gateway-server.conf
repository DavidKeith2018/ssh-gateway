# Localhost by default. Change the listeners and restart for remote access.
GATEWAY_SSH_LISTEN=127.0.0.1:2222
GATEWAY_WEB_LISTEN=127.0.0.1:8080
# Optional built-in HTTPS. The service must be able to read the certificate and key.
GATEWAY_TLS_CERT=
GATEWAY_TLS_KEY=
